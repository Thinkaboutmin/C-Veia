#include "string_table.hpp"

/* TODO: This should be generated!
 × ║ ○ ║ ×
═══╬═══╬═══
 × ║ × ║ ×
═══╬═══╬═══
 × ║ × ║ ×

 */
/*
void PlainTable::generateStringTable() {
    if (this->columns != 0 && this->rows != 0) {
        for (unsigned short row_x = 0; row_x < this->rows;++row_x) {
            this->str_table.emplace_back(std::string());
            this->str_table.emplace_back(std::string());
            for (unsigned short column_y = 0; column_y < this->columns - 1; ++column_y) {
                this->str_table[row_x] += " "
                this->str_table[row_x + 1] += "_|";
            }
        }
    }
}
*/